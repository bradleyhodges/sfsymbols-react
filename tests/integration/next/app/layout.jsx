export const metadata = { title: "SFIcon packed Next verification" };

export default function Layout({ children }) {
    return (
        <html lang="en">
            <body>{children}</body>
        </html>
    );
}
